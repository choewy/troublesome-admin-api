export enum UserType {
  Member = 'member',
  Manager = 'manager',
  Admin = 'admin',
}

export enum UserStatus {
  Activated = 'activated',
  Disabled = 'disabled',
}

export enum UserSearchKeywordField {
  Email = 'email',
  Name = 'name',
}
